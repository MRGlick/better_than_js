


int rand() {
    int sum = 0;
    int power = 1;
    int i = 0;
    while (i < 32) {
        if (maybe) sum += power;
        power *= 2;
        i += 1;
    }

    return sum;
}

int rand(int upper_bound) return rand() % upper_bound;

int rand(int lower_bound, int upper_bound) return lower_bound + rand(upper_bound - lower_bound);

struct Node {
    int val;
    Node next;
}

Node n = new Node(val = 2, next = new Node(3));

n = null;
