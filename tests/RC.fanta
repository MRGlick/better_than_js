


int rand() {
    int sum = 0;
    int power = 1;
    for (int i = 1; i < 32; i += 1; ) {
        sum += maybe * power;
        power *= 2;
    }

    return sum;
}

int rand(int bound) {
    return rand() % bound;
}

int rand(int lower, int upper) return lower + rand(upper - lower);

struct Node {
    int val;
    Node next;
}

Node lst = new Node(val = rand(50));
Node end = lst;
int i = 0;
while (i < 20) {

    end.next = new Node(val = rand(1, 100));
    end = end.next;

    i += 1;
}
print "Loop end!";



