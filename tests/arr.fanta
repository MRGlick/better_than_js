

// initializing with array literal
int[] int_arr = int[1, 2, 3, 4, 5];

float[] float_arr = float[6, 7, 8, 9, 10];

int[] empty = int[];


// 2d array with array literals
bool[][] board = bool[
    [false, true, false],
    [true, false, true],
    [false, true, false]
];

// 2d array with array initializer
bool[][] board2 = bool[3; 3];

for (int r = 0; r < board2.length; r += 1; ) {
    for (int c = 0; c < board2[i].length; c += 1; ) {
        board2[r][c] = (r + c) % 2 == 0;
    }
}
