int[] do_stuff(int[] arr) {
    int sum = 0;
    for (int i = 0; i < arr.length; i += 1; ) {
        sum += arr[i];
    }

    return [sum, arr.length];
}

int[] results = do_stuff([5, 6, 7, 8]);

print "Sum: ", results[0];
print "Count: ", results[1];
