
void map(int[] arr, int(int) f) {
    for (int i = 0; i < arr.length; i += 1; ) {
        arr[i] = f(arr[i]);
    }
}

int double(int x) return x * 2;

int print_ret(int x) {
    print "Val: ", x;
    return x;
}

int[] arr = [1, 2, 3, 4, 5];

map(arr, double);
map(arr, print_ret);
